apiVersion: v1
kind: Service
metadata:
  name: {{.Values.service.name}}
  namespace: {{.Values.namespace}}
spec:
  selector:
    app: {{.Values.deployment.name}}
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP